#pragma once
//#include <Windows.h>

enum Dir { UP, DOWN, LEFT, RIGHT, NODIR};

//Dir c2d(char);

int random(int a, int b);

//COORD random(COORD a, COORD b);

//inline
//bool operator==(const COORD &lhs, const COORD &rhs)
//{
//	return lhs.X == rhs.X && lhs.Y == rhs.Y;
//}